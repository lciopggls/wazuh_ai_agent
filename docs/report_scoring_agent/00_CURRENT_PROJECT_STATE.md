# 报告评分智能体：当前项目状态

> 更新日期：2026-08-20  
> 当前分支：`codex/report-scoring-agent-planning`  
> 远端基线：`b89f4ae`（已推送到同名远端分支）  
> 当前阶段：小阶段开发与人工验收完成，等待独立代码审查

本文档是报告评分子项目的状态事实源。历史计划、阶段截图、哈希清单和临时验收记录已清理；需要长期保留的结论统一记录在本文档和实现交接文档中。

## 1. 业务边界

- 报告评分智能体只评价已经生成的攻击溯源报告，不参与攻击执行、Wazuh 告警采集或溯源报告生成。
- 当前评分标准固定为 `v3.0`，总分 100 分，包含六个评分维度、优点、缺点、根因归纳和负向行为扣分。
- DeepSeek 是当前评分模型提供方；评分结果属于“评分智能体的模型评审结果”，不是人工分数，也不是被评分智能体自己的自评分。
- 当前目标是先保证 LangGraph Studio 和项目完整前后端两条调用链稳定可用。模型分数与人工分数的校准不属于本小阶段验收条件。

## 2. 已实现能力

- LangGraph 已注册 `report_scoring_agent`，支持在 Studio 中提交 Human Input 并查看最终状态、AIMessage 和结构化分数。
- 后端提供报告登记、Studio 导入、报告列表、评分/重评分、评分历史、最新分数、分数详情和多智能体对比接口。
- 报告、评分 attempt 和最终结果使用文件持久化，并具有请求幂等、重评分和失败状态记录。
- 模型输出经过结构化解析、字段校验、分数重算和负向行为规则处理后才持久化。
- 前端评分入口受 `VITE_ENABLE_REPORT_SCORING=true` 控制，可完成报告登记、发起评分、查看六维详情、历史和对比。
- 数智运维工作台新增开发测试模块，受 `VITE_ENABLE_TEST_MODULE` 控制；正式 AI 智能助手保留仅使用路由智能体的对话窗口，测试模块可选择攻击溯源智能体和两个基线智能体，并使用独立会话存储。
- 报告评分入口已从正式 AI 智能助手迁移到测试模块；`VITE_ENABLE_REPORT_SCORING` 继续保留，用于控制测试模块中的评分功能。
- 功能导航按分组数组纵向渲染；关闭 `VITE_ENABLE_TEST_MODULE` 后“测试”分组不生成，后续分组会自动上移，不会留下固定空位。
- 已内置 SIM-204、SIM-205、SIM-206 测试用例与评分标准目录。

## 3. 验证状态

### 自动化验证（本阶段最后一次记录）

- `uv run ruff check . --fix`：通过。
- `uv run pytest`：145 passed，2 warnings。
- 前端测试、类型检查和生产构建：通过。
- 本次测试工作台优化后，前端类型检查、报告评分前端测试和生产构建通过；后端自动化测试因本机 `.venv` 指向已不存在的 Python 解释器，尚未重新执行。
- 用户已要求暂停后续批量测试；文档整理不重新运行测试。

### 人工验收

- LangGraph Studio：图、节点和输入均可用，运行成功结束，示例总分为 83.0，无页面或终端错误。
- 完整前后端：后端 `http://127.0.0.1:8001`、前端实际地址 `http://localhost:8113/#/index`。
- `VITE_ENABLE_REPORT_SCORING=true` 生效，SIM-204 / `baseline_agent_simple` 登记和评分成功。
- 人工验收记录：
  - `report_id=rpt_b15eef461618410e97ea696d076803d8`
  - `attempt_id=att_0f77305811024bf69d2e596982fda380`
  - `score_id=scr_d1c9ffae461a4e22972bb760c492d8b3`
  - `total_score=48.0`
- 六个评分维度、优缺点、历史和多智能体对比均正常显示；刷新页面没有产生重复 attempt。
- 未评分报告访问最新分数接口返回 404，属于设计内的预期状态。
- 用户已确认本次“测试”模块手动测试通过。
- 验收结束后 LangGraph dev、前端和 8001 后端均已停止；额外 Wazuh 服务状态不属于本子项目验收范围。

## 4. Git 交付状态

已推送提交：

- `b016003 build: add multipart form parsing dependency`
- `cbb76cd feat: add report scoring agent`
- `b89f4ae 修复报告评分工作流集成问题`

远端分支已包含本阶段正式项目代码。本目录的状态文档仍是本地文档，未收到提交授权前不得自行提交或推送。

## 5. 已知事项与延后范围

- 小批量流程已证明可用，不再完整执行剩余批量评分（原 M3.3 范围）。
- 自动分数与人工参考分数存在差异，SIM-204 曾观察到约 +18 分偏差；校准、提示词优化和评分一致性分析延后处理。
- 当前重点不是继续扩大测试量，而是完成独立代码审查并修复审查确认的问题。

## 6. 工作区边界

当前工作树仍包含既存的本地修改和未跟踪测试、仿真数据、知识图谱输入及其他文件。这些内容没有纳入上述远端提交，也不得在本任务中清理、覆盖或提交。后续提交仍需用户明确授权，并只包含用户批准的项目文件。

## 7. 下一步

1. 代码审查人员以远端分支和 `b89f4ae` 为基线完成独立审查。
2. 如有有效审查意见，代码编写人员只修改对应代码并同步本文档与当前任务文档。
3. 审查闭环后，由项目计划人员决定下一阶段范围；在此之前不主动恢复批量测试或分数校准工作。
