[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "wazuh_ai_agent"
version = "0.0.1"
authors = [
    { name="KIDPhantom", email="xiangkaicheng@foxmail.com" }
]
description = "An AI agent interacting with Wazuh API"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.116.1",
    "langchain>=1.2.6",
    "langchain-core>=1.2.7",
    "langchain-huggingface>=1.2.0",
    "langchain-openai>=1.1.7",
    "langchain-pinecone>=0.0.1",
    "langgraph>=1.0.6",
    "pinecone>=8.0.0",
    "pydantic-settings>=2.12.0",
    "python-multipart>=0.0.20",
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
    "uvicorn>=0.40.0",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]

[project.scripts]
wazuh-topology-api = "service.topology_service:main"

[tool.hatch.build.targets.sdist]
include = [
  "src/*",
  "tests/*",
]
exclude = []

[tool.hatch.build.targets.wheel]
packages = ["src/core", "src/wazuh_api", "src/agents", "src/service"]

# =========== Tool Configurations ============

# Use uv to manage development environment, uv add --dev pkg
[dependency-groups]
dev = [
    "agentevals>=0.0.9",
    "black>=26.1.0",
    "langgraph-cli[inmem]>=0.4.11",
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
    "requests-mock>=1.12.1",
    "ruff>=0.14.13",
]

[tool.coverage.run]
omit = ["__*__.py", "*.pyc"]

# uv run black .
[tool.black]
line-length = 100
target-version = ["py311", "py312", "py313"]
exclude = '''
/(
    \.git
    | \.pytest_cache
    | \.ruff_cache
    | \.venv
    | dist
)/
'''

# uv run ruff check . --fix
[tool.ruff]
line-length = 100
target-version = "py311"
exclude = [
    ".git",
    ".ruff_cache",
    ".pytest_cache",
    ".venv",
    "dist",
]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # Pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
]
ignore = [
    "E501",  # line too long (handled by black)
    "B008",  # do not perform function calls in argument defaults
    "C901",  # too complex
]

[tool.pytest.ini_options]
filterwarnings = [
    "ignore::urllib3.exceptions.InsecureRequestWarning",
]
